{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@lenotes-ng/api",
	"projectType": "application",
  "targets": {
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start api"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --rootDir=apps/api/"
      }
    }
  }
}
